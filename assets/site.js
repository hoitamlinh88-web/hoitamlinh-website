
const menuButton=document.querySelector('.mobile-menu');const nav=document.querySelector('header nav');if(menuButton&&nav){menuButton.addEventListener('click',()=>{const open=nav.classList.toggle('open');menuButton.setAttribute('aria-expanded',open?'true':'false')})}const searchInput=document.querySelector('#site-search');const results=document.querySelector('#search-results');const dataNode=document.querySelector('#search-data');let pages=[];try{pages=JSON.parse(dataNode.textContent||'[]')}catch(e){}if(searchInput&&results){searchInput.addEventListener('input',()=>{const q=searchInput.value.trim().toLowerCase();results.innerHTML='';if(q.length<2)return;pages.filter(p=>(p.title+' '+p.text).toLowerCase().includes(q)).slice(0,8).forEach(p=>{const a=document.createElement('a');a.href=p.url;a.textContent=p.title;results.appendChild(a)})})}

(async()=>{try{const accountLink=[...document.querySelectorAll('header nav a')].find(link=>link.getAttribute('href')==='dang-nhap.html');if(!accountLink)return;const{getAccess,destinationForRole}=await import('./auth.js');const access=await getAccess();if(!access.user||!access.profile)return;accountLink.textContent='Tài khoản';accountLink.href=destinationForRole(access.profile.role);accountLink.title=access.profile.display_name||access.profile.email||access.user.email||'Tài khoản';accountLink.dataset.accountLink='true'}catch(error){}})();
